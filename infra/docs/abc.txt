Explicitly Out Of Scope For Now
Deployment
Long-term memory
RAG
Notes
Document upload
Web search
Third-party integrations
Agents
TypeScript frontend

ssh-keygen -t ed25519 -C "satyamkr1354@gmail.com"
---------------------------------------------------
cd c:\Users\Satyam Kumar\Desktop\OneAI
docker-compose up -d

cd apps/api
pip install -e ".[dev]"  # Install dependencies
alembic upgrade head      # Run migrations
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000